#cloud-config
users:
  - name: vmuser
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users,adm
    shell: /bin/bash

package_update: true
packages:
  - qemu-guest-agent

runcmd:
  - [ sh, -c, 'echo cloud-init placeholder > /var/log/cloud-init.log' ]
